import React from 'react';
import styled from 'styled-components';

export const SignUp = () => {
  return (
    <SContainer>
      <h1>Yo sign yo self up</h1>
    </SContainer>
  );
};

const SContainer = styled.main``;
